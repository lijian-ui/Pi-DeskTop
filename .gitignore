# ── 依赖与构建产物（体积大 / 可再生）──
node_modules/
dist/
release/

# ── 发布脚本（含 Gitee/GitHub 令牌，严禁提交！）──
# 以下脚本含真实 token / 关联凭证，精确排除；不含密钥的 scripts/build-electron.mjs 正常入库
# 注意：今后在 scripts/ 新增含 token 的脚本，须在此处补一行精确排除
scripts/publish-lib.mjs
scripts/publish-github.mjs
scripts/publish-gitee.mjs
scripts/publish.mjs

# ── 内置 Node 绿色包 / 安装包（几十~上百 MB，不应入库）──
node-v24.18.0-win-x64/
node-v24.18.0-x64.msi
node-v24.18.1-win-x64/
node-v24.18.1-win-x64.zip
node-v24.18.1-darwin-x64/
node-v24.18.1-darwin-x64.tar.gz

# ── 工具/IDE 元数据 ──
.workbuddy/
.arts/
.codeartsdoer/
.idea/
.vscode/

# ── 系统 / 编辑器 ──
.DS_Store
Thumbs.db
*.log
*.pid
npm-debug.log*

# ── Windows 误创建 ──
nul

# ── 参考项目（仅本地分析用，不纳入版本）──
参考项目/dingtalk-openclaw-connector/
参考项目/tencent-weixin-openclaw-weixin-2.4.3

# ── 本地临时文件 ──
*.tmp
*.bak

# ── 宣传视频 / 截图等媒体素材（体积大，不入库）──
video/
