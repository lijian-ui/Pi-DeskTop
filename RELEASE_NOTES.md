# Pi Desktop v0.4.0 Release Notes

## ✨ 新功能

### IM 网关（钉钉机器人接入）
- 新增「IM 接入」导航页：配置钉钉机器人（渠道名称 / AppKey / AppSecret）、启用 / 停用、连接状态实时展示，支持单聊与群聊。
- **AI 卡片流式输出**：钉钉端回复实时流式呈现（AI 卡片），工具执行期间保持"输入中"，回复结束自动定格；长回复节流合并发送，不再被平台截断。
- **斜杠命令**（钉钉内直接发送）：`/model` 查看已配置模型、`/model <名称或编号>` 切换会话模型；`/compact` 压缩上下文；`/help` 命令帮助；`/reset` `/clear` `/new` 开启新会话。
- **默认工作区**：每个渠道可配置默认工作目录，机器人会话的 AI 可直接读写该目录下的项目文件；未配置时使用内置聊天目录。
- **会话迁移**：已建立的 IM 会话可整体迁移到新工作目录，**对话历史保留、会话身份不变**（同一会话在桌面端与钉钉端连续）。两个入口：IM 接入页按渠道批量迁移、桌面会话窗口"选择工作目录"单会话迁移（正在回复的会话自动跳过）。
- **侧栏辨识**：IM 会话在侧栏显示为「[渠道名] 首条消息」，一眼区分来源；会话按工作目录归入「空间」分组。
- 渠道卡片操作强化：编辑 / 删除 / 启停开关 / 批量迁移会话 / 配置工作区。

### 定时任务（自动化）
- 新增「自动化」一级导航与定时任务管理页：创建 / 编辑 / 启用暂停 / 删除 / 立即运行。
- 三种调度类型：**每天**（指定时间）、**间隔**（按分钟）、**单次**；30 秒心跳调度，防重入，错过运行窗口按配置策略自动处理。
- 任务编辑支持：提示词、附加规则（随任务注入 `<scheduled_task>` 系统提示词块）、执行模型独立选择、权限配置（YOLO 全自动 / 询问仅白名单，危险命令黑名单始终强制）。
- 运行历史自动归档：**一个任务 = 一个累计会话**，存于标准会话目录；侧栏「定时任务」分组点击即开，支持继续追问、导出、重命名、删除；运行中显示转圈，执行事件实时推送。
- 任务配置独立存放 `~/.pi/agent/scheduled/`（scheduled-tasks.json / scheduled-runs.json / scheduled-state.json），启动自动迁移旧位置文件。

### 多模态图片输入
- 对话输入框支持**粘贴（Ctrl/Cmd+V 剪贴板截图）/ 文件上传 / 拖拽**三种方式添加图片。
- 待发送图片以**胶囊缩略图**展示，可逐个移除；发送时以 base64 随消息传给模型（Pi SDK 原生支持 `prompt(text, { images })`，主进程 / preload 零改动）。
- 消息内渲染图片缩略图，**点击可看大图**；图片从 SDK `message_start` 事件与会话历史的 `image` content block 解析，**切换/重开会话后缩略图依然保留**。
- 流式队列场景透传图片；支持多模态模型（Qwen-VL / GPT-4o / Claude 等）"看到"图片，纯文本模型自动忽略不崩溃。

### 规则与记忆（设置）
- 原「记忆」占位页升级为「**规则与记忆**」页面。
- **导入设置**：AGENTS.md、CLAUDE.md（含 CLAUDE.local.md）上下文文件导入开关，拨动即保存；**默认均不注入**，由用户自行开启。开启后按目录链自动发现并注入系统提示词（AGENTS 与 CLAUDE 可同时注入）。
- **规则**：单一 `rules.md` 文件管理全部规则，支持创建 / 编辑 / 删除；规则以 `<rules>` 块注入**系统提示词最末尾**，所有会话（含定时任务）遵循；每轮重读，保存后下一条消息即生效。

### Pi 扩展商店
- 新增一级导航「**扩展**」，对标 pi.dev 的扩展市场：**市场 / 已安装**双 Tab。
- **市场**：数据直接来自 npm registry（`keywords: pi-package`），支持关键字搜索、按下载量排序、**无限滚动分页**（触底自动加载 + 「加载更多」兜底，可浏览成百上千个包）。
- **包详情弹窗**：点击任意卡片弹出，展示完整元信息（版本 / 发布时间 / 作者 / 许可证 / 体积 / 依赖数 / 月下载 / 周下载量）+ **README 全文**（Markdown 渲染），并可直接安装、跳转 npm / 主页 / GitHub。
- **已安装**：列出已安装扩展（支持 git 源），一键卸载；npm 源的已安装卡同样可点开详情；详情中对已装包显示「已安装」状态。
- 安装机制直接复用 **Pi SDK 的包管理器**（无需 `pi` CLI）：包下载到 `~/.pi/agent/npm/node_modules/`，配置写入 `settings.json` 的 `packages`；装/卸后清缓存，下一会话生效（暂只支持全局安装）。

### 帮助与反馈
- 新增「帮助」页面：新手引导（快速上手）、常见问题（FAQ）、资源链接（官方文档 / 扩展市场 / 版本发布页）、反馈渠道（问题上报），并接入原生菜单入口。
- 帮助页内所有外部链接统一走系统默认浏览器，不弹应用内窗口。

## 🐛 问题修复

- **定时任务会话点击无反应**：会话文件改存标准 `sessions/` 目录，可被正常发现与打开。
- **运行后侧栏分组不出现（需手动刷新）**：运行事件同步刷新会话列表。
- **第二次运行结果不显示（需整页重载）**：定时任务运行事件接入统一事件管道，内容实时累积。
- **运行完成后仍转圈**：运行记录引入唯一 ID，状态准确落盘。
- **定时任务默认工作目录误用 chat 目录导致会话重复渲染**：默认改为空，未绑定工作空间时运行于 `~/.pi/agent/cron` 兜底目录。
- **外部链接打开应用内新窗口**：所有 http/https 外链（详情页链接、README、LLM 回复中的链接等）统一改用**系统默认浏览器**打开，不再弹出应用内窗口。
- **扩展市场搜索 400**：搜索关键字与基础查询参数冲突（重复 text 参数），改为替换式拼接。
- **钉钉机器人连接报错（bufferutil）**：`ws` 可选原生依赖残留打包导致连接启动失败，改为运行时解析整条依赖链。
- **钉钉消息无响应**：机器人订阅声明晚于连接握手，服务端不下发消息；调整注册时序后正常收发。
- **钉钉"回声"与回复丢失**：SDK 对用户消息也会发 `message_end`，回复路由按角色过滤后修复。
- **IM 会话点开不渲染 / 切模型不生效**：会话事件改为始终广播（渲染端按会话累积）；模型切换显式携带会话工作目录。
- **钉钉卡片卡"处理中" / 长回复截断 `***`**：结束信号改用回复周期终点（agent_end）；流式发送节流合并，规避平台单帧限制。
- **IM 会话迁移后 AI 仍在旧目录工作**：会话工作目录以会话文件自身 header 为准（迁移、渠道配置均不覆盖），钉钉端与桌面端一致。
- **`/model` 无参报错**：模型列表超长触发平台消息长度限制，改为只列已配置模型并自动拆条发送。
- **`/model` 供应商名显示为模型名**：展示层增加品牌名映射与脏数据兜底（provider 名称等于模型 ID 时回退显示供应商 ID）。

## 📦 构建

- 版本号：`0.4.0`
- Electron：`^35.0.0`
- Pi SDK：`^0.83.0`
- node-pty：`^1.1.0`
- React：`^19.1.0`
- 新增依赖：`dingtalk-stream`（钉钉机器人长连接）、`axios`（钉钉开放平台 API）

